# CLAUDE.md — gitstats

> **Versioning**: this file is committed alongside code changes — git history is the
> version history. Update it in the same commit as the code it describes. On revert,
> `CLAUDE.md` reverts with the code automatically.

---

## What this project is

`gitstats.py` — a single-file Python script that analyzes a git repository and produces
a self-contained, dark-themed HTML report with Chart.js charts and interactive tables.

**Output**: one `index.html` file, no external dependencies at runtime.

**Usage**: `python3 gitstats.py <repo_path> [output_dir] [--blame [cache_dir]]`

---

## Repo layout

```
gitstats.py            # main script — all HTML/CSS/JS is generated inline here
docs/
  screenshot.py        # Playwright script: generates obfuscated sample + screenshots
  sample_report.html   # obfuscated demo report (committed, regenerated by screenshot.py)
  screenshot-*.png     # section screenshots used in README
  CLAUDE.md            # this file — updated with each commit
README.md
```

---

## Features implemented

- Commit count matching GitHub (`git rev-list HEAD --count`, not `--all`)
- Per-author commit list modal: click author name → modal with hash, date, message, GitHub link
- GitHub remote URL auto-detected (SSH + HTTPS)
- Lazy section init: charts only render when their nav tab is first clicked
- Chart.js charts: activity (hour/dow/month/year/monthly-line), heatmap, authors bar,
  contributions per-author, files-over-time, LOC-over-time
- Obfuscation + screenshot pipeline (`docs/screenshot.py`)
- **Compare tab**: side-by-side author comparison — commits/month, hour/dow activity,
  cumulative net-LOC, cumulative additions
- **`--blame` mode**: git-blame-based actual LOC per author over time (who wrote the lines
  currently present in the project, accounting for deletions by others)
  - Cache: `{cache_dir}/{repo_id}_{hash}_{filter_sig}.json` — keyed by filter fingerprint
    so config changes auto-invalidate stale entries
  - Filter config: `{cache_dir}/blame_filter.ini` — auto-created on first run, program
    exits so user can review before blame starts
  - Binary files always excluded (via `git diff --numstat` against empty tree)
  - Config supports: `[whitelist]`/`[blacklist]` extensions, `[path_whitelist]`/`[path_blacklist]`
    patterns (prefix `/` for repo-root-anchored, else substring); path whitelist overrides blacklist
  - Blame charts: line chart (selected authors, clickable points → commit panel),
    bar chart (all authors at latest snapshot)
  - Commit drill-down panel: click a point → sortable table of commits for that bucket;
    freeze button locks panel while browsing

---

## Screenshot rendering — root cause + fix

**Problem**: Charts rendered inside `display:none` sections measure 0-width containers →
broken axis layouts (rotated labels eating vertical space, flat bars).

**Fix** (in `docs/screenshot.py`):
1. Patch HTML before loading: inject `<style>.section{display:block!important}</style>`
2. Call every `sectionInits[sec]()` after load — all charts render at correct 1400px width
3. Scroll full page so all content renders
4. Screenshot each section by PIL-compositing `header` + `nav` + `#sec-{name}` element
   screenshots — no show/hide toggling, no chart resize triggered

---

## Obfuscation rules (docs/screenshot.py)

- Author names: `AUTHOR_MAP` dict (real → fake)
- Short hashes `<code>` tags: deterministic MD5 fakes
- Full hashes in JSON: SHA1 fakes
- Commit messages: only fix unintelligible/revealing ones via `MESSAGE_FIXUPS`
- `githubBase` cleared to `""` — modal links don't point to real commits
- Repo path → `/private/REDACTED_REPO`, remote URL → `[private repo]`

---

## Rules

- **NEVER auto-push** — always ask before `git push`
- Default screenshot repo: `/private/repo`
- `docs/screenshot.py` requires `pillow` + `playwright` (`pip install pillow playwright`)
